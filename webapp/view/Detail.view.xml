<mvc:View
    controllerName="bs.ui5.projeto.controller.Detail"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.ui.layout.form"
    xmlns:l="sap.ui.layout"
    height="100%">

    <Page
        id="detailPage"
        title="{nome}"
        showNavButton="true"
        navButtonPress="onNavBack">

        <headerContent>
            <Button
                id="addResourceBtn"
                text="{i18n>addResource}"
                icon="sap-icon://add"
                type="Emphasized" 
                press="onAddResource" />
        </headerContent>

        <content>
            <VBox class="sapUiSmallMargin">
                
                <Title 
                    text="{i18n>sectionResources}" 
                    level="H3" 
                    titleStyle="H4"
                    class="sapUiSmallMarginBottom" />

                <Table 
                    id="resourcesTable" 
                    items="{path: 'recursos'}"
                    growing="true"
                    growingThreshold="10"
                    sticky="HeaderToolbar,ColumnHeaders">
                    <columns>
                        <Column width="25%"><Text text="{i18n>field.title}" style="Bold"/></Column>
                        <Column width="15%"><Text text="{i18n>field.type}" /></Column>
                        <Column width="35%"><Text text="{i18n>field.description}" /></Column>
                        <Column width="10%" hAlign="Center"><Text text="{i18n>field.mandatory}" /></Column>
                        <Column width="15%" minScreenWidth="Tablet" demandPopin="true" hAlign="End">
                            <Text text="{i18n>field.url}" />
                        </Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{titulo}" wrapping="true" renderWhitespace="true" class="sapUiSelectable" />
                                <Text text="{tipo}" />
                                <Text text="{descricao}" wrapping="true" />
                                <CheckBox selected="{obrigatorio}" editable="false" />
                                <Link text="{i18n>openResource}" href="{url}" target="_blank" />
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </VBox>
        </content>
    </Page>
</mvc:View>